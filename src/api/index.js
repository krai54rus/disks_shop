import config from "../config";
export function getUniqueId() {
    // if (!$cookies.isKey("clientId")) {
        // fetch(`${config.apiUrl}/users/uniqueId`, {
        //     method: "GET",
        //     credentials: "include",
        //     mode: "cors",
        // })
        // .then((response) => response.json())
        // .then((res) => {
        // if (res.status === "OK") {
        //     $cookies.set("clientId", res.result, 60 * 60 * 24 * 30);
        // }
        // });
    // }
}